---
const {trackName} = Astro.props
---

<audio controls>
  <source src=`../src/audio/${trackName}.mp3`>
  <source src=`../src/audio/${trackName}.wav`>
  This browser does not support HTML5 Audio! );
</audio>